import { useState } from "react";

const Refrec = () => {
  return (
    <div>
      {inventario}

      <div className="buttons">
        <button>Refresh</button>
        <button>Recicle</button>
      </div>
    </div>
  );
};

export default Refrec;
